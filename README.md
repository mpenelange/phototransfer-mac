# Photo Transfer

Photo Transfer is a native macOS app for safely importing photos from SD cards and mounted cameras. It organizes RAW and JPEG files into separate destinations, supports verified backups, groups matching photo pairs, and confirms every copy before optionally removing the originals.
